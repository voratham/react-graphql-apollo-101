# react-graphql-apollo-101

Try code with GraphQL , Apollo , ReactJS By The Project just basic connect with client to server api

```sh
> npm install
> cd ./clinet && npm install
> cd ./server && npm install
> npm run dev
```
